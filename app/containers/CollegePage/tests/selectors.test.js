// import { fromJS } from 'immutable';
// import { makeSelectCollegePageDomain } from '../selectors';

// const selector = makeSelectCollegePageDomain();

describe('makeSelectCollegePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
