// import { fromJS } from 'immutable';
// import { makeSelectChartDomain } from '../selectors';

// const selector = makeSelectChartDomain();

describe('makeSelectChartDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
