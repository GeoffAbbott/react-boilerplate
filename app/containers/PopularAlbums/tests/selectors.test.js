// import { fromJS } from 'immutable';
// import { makeSelectPopularAlbumsDomain } from '../selectors';

// const selector = makeSelectPopularAlbumsDomain();

describe('makeSelectPopularAlbumsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
