// import { fromJS } from 'immutable';
// import { makeSelectProductDetailsPageDomain } from '../selectors';

// const selector = makeSelectProductDetailsPageDomain();

describe('makeSelectProductDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
