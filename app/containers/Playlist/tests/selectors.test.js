// import { fromJS } from 'immutable';
// import { makeSelectPlaylistDomain } from '../selectors';

// const selector = makeSelectPlaylistDomain();

describe('makeSelectPlaylistDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
