// import { fromJS } from 'immutable';
// import { makeSelectBandPageDomain } from '../selectors';

// const selector = makeSelectBandPageDomain();

describe('makeSelectBandPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
