// import { fromJS } from 'immutable';
// import { makeSelectListArtistDomain } from '../selectors';

// const selector = makeSelectListArtistDomain();

describe('makeSelectListArtistDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
