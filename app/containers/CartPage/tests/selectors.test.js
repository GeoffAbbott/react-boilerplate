// import { fromJS } from 'immutable';
// import { makeSelectCartPageDomain } from '../selectors';

// const selector = makeSelectCartPageDomain();

describe('makeSelectCartPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
